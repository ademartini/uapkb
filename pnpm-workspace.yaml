# pnpm 10.x supply-chain hardening.
# pnpm 11 renames onlyBuiltDependencies → allowBuilds and changes minimumReleaseAge default.
packages:
  - "."

minimumReleaseAge: 1440

onlyBuiltDependencies:
  - esbuild

overrides:
  esbuild: "0.28.1"
