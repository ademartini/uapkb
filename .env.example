# App runtime
PORT=3000
LOG_LEVEL=info
APP_VERSION=
APP_COMMIT=

# Database (optional until Neon is provisioned)
DATABASE_URL=postgres://uapkb:uapkb@localhost:5432/uapkb

# Cursor CLI (CI only — use `agent login` locally)
CURSOR_API_KEY=

# Playwright smoke tests against deployed environments
PLAYWRIGHT_BASE_URL=

# fly.io deploy tokens (GitHub Environment secrets — never commit)
# FLY_API_TOKEN_STAGING=
# FLY_API_TOKEN_PRODUCTION=
