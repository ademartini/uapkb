{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "name": "UAPKB Cursor Cloud",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "install": "pnpm install --frozen-lockfile"
}
